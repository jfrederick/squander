# Routine state (autonomous dev-loop)

Read this first on every scheduled run. Update it before ending every run — this is the only thing carrying context forward between sessions.

## Schedule

- Cadence not yet fixed by the user. Default assumption until told otherwise: most runs do feature ideation → build; **twice a week, do a technical-maintenance research pass instead** (proposals only, <200 words each, no build).
- Maintenance days chosen so far: none yet — pick two non-adjacent days once the user confirms a schedule, then record them here.

## Status

- **2026-07-05 — Run 1 (this run).** Repo has zero implemented app code; only the `add-spending-tracker-mvp` OpenSpec change (proposal/design/specs/tasks) is merged, 0/~30 tasks checked off.
  - Did NOT fabricate a "new features" list for an app that doesn't exist. Surfaced this to the user via notification along with 5 candidate feature ideas (post-MVP) and asked for direction: build the MVP first, ideate anyway, or something else.
  - Set up `CLAUDE.md` (project context) and this file (routine memory) on `claude/sharp-hypatia-m2a713`, pushed, no PR yet (nothing user-approved to merge).
  - No worktrees created, no features built, no PRs opened.

## Open questions for the user (pending reply)

1. Build the MVP (existing `tasks.md`) before/instead of new-feature ideation this cycle?
2. Which 2 weekdays should be "maintenance" days?
3. Confirm the 5 proposed feature ideas (see notification) or provide different priorities.

## In-flight work (worktrees / branches / PRs)

_None yet._

## Log

- 2026-07-05: Run 1 — infra setup + blocked on user reply (see above).
